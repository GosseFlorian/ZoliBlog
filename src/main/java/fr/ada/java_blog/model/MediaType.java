package fr.ada.java_blog.model;

/**
 * Types de média — aligné sur l'enum PostgreSQL {@code type} ({@code image}, {@code video}, {@code
 * gif}, {@code musique}).
 */
public enum MediaType {
  image,
  video,
  gif,
  musique
}
